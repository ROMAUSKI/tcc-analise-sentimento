# === Backups e ZIPs ===
_backup/

# Ignorando datasets reais pesados (reproduzíveis via kagglehub)
src/dados_reais/
src/dados_reais_temp/
src/utlc_movies.zip
src/*.zip
src/*.csv

# Modelos/checkpoints pesados de fine-tuning (BERT, LSTM)
src/checkpoints_avancado/
src/checkpoints_*/

# === Documentos UTFPR (regulamentos, templates, checklists) ===
documentos/regulamentos/
documentos/*.pdf
documentos/referencias/*.pdf

# === Jupyter ===
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# === Python ===
__pycache__/
*.pyc
.env
venv/

# === LaTeX build ===
# Arquivos temporários do LaTeX
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.lof
*.lot

# Arquivos de sistema do Windows
Thumbs.db
desktop.ini

# === Google Drive (pasta temporária de sincronização) ===
.tmp.driveupload/

# === OS ===
.DS_Store
Thumbs.db
desktop.ini
.claude

# === IDE / Editor ===
.vscode/
